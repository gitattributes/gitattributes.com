{
  "name": "gitattributes.io",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "del": "^6.0.0",
    "gulp": "^4.0.2"
  },
  "scripts": {
    "build": "gulp build",
    "clean": "gulp clean"
  }
}
