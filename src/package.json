{
  "name": "gitattributes.io",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=10"
  },
  "devDependencies": {
    "del": "^4.1.1",
    "gulp": "^4.0.2"
  },
  "scripts": {
    "build": "gulp build",
    "clean": "gulp clean"
  }
}
